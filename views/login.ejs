<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      body {
        background-color: #f7fafc;
        font-family: Arial, sans-serif;
        margin: 0;
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        position: relative;
      }

      .dark-mode {
        background-color: #2d3748;
        color: #f7fafc;
      }

      /* Container của bảng thông báo Welcome to E-shop */
      .marquee-container {
        width: 100%;
        height: 100vh; /* Đổi từ 100% thành 100vh để chiếm toàn bộ chiều cao màn hình */
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(to right, #32fe35, #c9ee22); /* Nền gradient */
        color: #ffffff; /* Màu chữ trắng */
        font-size: 40px;
        font-weight: bold;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Thêm bóng cho bảng */
        margin: 0; /* Bỏ margin */
        padding: 0; /* Bỏ padding */
      }


      .container {
        background-color: #ffffff;
        padding: 32px;
        border-radius: 8px;
        width: 100%;
        max-width: 400px;
        height: auto;
        position: absolute;
        top: 20%;
        right: 5%;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Thêm bóng cho form */
      }

      h2 {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 24px;
        text-align: center;
      }

      label {
        display: block;
        color: #4a5568;
        margin-bottom: 8px;
      }

      input {
        width: 100%;
        padding: 12px;
        border: 1px solid #cbd5e0;
        border-radius: 8px;
        margin-bottom: 24px;
        outline: none;
      }

      input:focus {
        border-color: #4299e1;
        box-shadow: 0 0 0 2px rgba(66, 153, 225, 0.5);
      }

      button {
        width: 100%;
        background-color: #4299e1;
        color: #ffffff;
        padding: 12px;
        border-radius: 8px;
        border: none;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.2s;
      }

      button:hover {
        background-color: #2b6cb0;
      }

      p {
        margin-top: 16px;
        text-align: center;
      }

      p a {
        color: #4299e1;
        text-decoration: none;
      }

      p a:hover {
        text-decoration: underline;
      }

      .message {
        margin-top: 16px;
        padding: 16px;
        background-color: #fed7d7;
        color: #c53030;
        border-radius: 8px;
      }

      .input-container {
        position: relative;
        margin-top: 20px;
        width: 100%;
      }

      input[type="email"],
      input[type="password"] {
        width: 100%;
        padding: 12px 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 4px;
        outline: none;
        background-color: transparent;
      }

      input[type="email"]:focus,
      input[type="password"]:focus {
        border-color: #007bff;
      }

      label {
        position: absolute;
        top: 34%;
        left: 10px;
        transform: translateY(-50%);
        font-size: 16px;
        color: #999;
        transition: all 0.3s ease;
        pointer-events: none;
        background-color: white;
        padding: 0 5px;
      }

      input[type="email"]:focus + label,
      input[type="email"]:not(:placeholder-shown) + label,
      input[type="password"]:focus + label,
      input[type="password"]:not(:placeholder-shown) + label {
        top: -7px;
        left: 10px;
        font-size: 14px;
        color: #333;
      }
    </style>
  </head>

  <body>
    <!-- Bảng thông báo Welcome to E-shop -->
    <div class="marquee-container">
      <div style="margin-left: -700px;">
        Welcome to E-shop
      </div>
    </div>

    <!-- Container chính chứa form login -->
    <div class="container">
      <h1 style="text-align: center; font-size: 50px; color: #333;">Login</h1>
      <form action="/auth/login" method="POST">
        <div class="input-container">
          <input type="email" id="email" name="email" required placeholder="" />
          <label for="email">Your email</label>
        </div>
        <div class="input-container">
          <input type="password" id="password" name="password" required placeholder=" " />
          <label for="password">Password</label>
        </div>
        <button type="submit">Login</button>
      </form>
      <p style="color: #333; font-size: 18px;">
        Don't have an account? <a href="/register">Register</a>
      </p>
      <% if (typeof message !=='undefined' && message) { %>
      <div class="message"><%= message %></div>
      <% } %>
    </div>
  </body>
</html>
